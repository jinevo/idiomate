const Reasons = {
    device:
        'Your device is not currently compatible with the current app. Please try with Google Chrome Browser',
    audio:
        'You must enable the audio and authorize the app to access it in order to use the app',
};

export default Reasons;
